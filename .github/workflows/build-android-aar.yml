name: Build Android AAR

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'sdk-android/**'
      - '.github/workflows/build-android-aar.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Build release AAR
        working-directory: sdk-android
        run: |
          chmod +x ./gradlew
          ./gradlew :monetize-sdk:assembleRelease

      - name: Publish AAR into dist
        shell: bash
        run: |
          mkdir -p dist/android
          cp sdk-android/monetize-sdk/build/outputs/aar/monetize-sdk-release.aar \
             dist/android/PocketsFullMonetize-1.0.0.aar

      - name: Commit generated AAR
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist/android/PocketsFullMonetize-1.0.0.aar
          if git diff --cached --quiet; then
            echo "Android AAR is already current."
            exit 0
          fi
          git commit -m "Build Android v1.0.0 AAR"
          git push
